apiVersion: v1
kind: Service
metadata:
  name: demo-nodejs-service
  namespace: argocd
  labels:
    app: demo-nodejs
spec:
  ports:
    - port: 8080
      nodePort: 30700
  selector:
    app: demo-nodejs
